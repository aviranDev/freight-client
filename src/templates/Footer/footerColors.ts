import { color } from "../../styles/colors";
const { primary, text1, text2, secondary } = color;

export const clrs = {
  bgc: primary,
  bTop: text1,
  border: "rgba(255, 132, 132, 0.2)",
  linksText: text2,
  iconClr: secondary
};

